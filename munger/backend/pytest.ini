[pytest]
testpaths = tests
addopts = -m "not integration"
markers =
    integration: tests that exercise live or multi-step integration flows
    provider: tests that require model-provider access
    live_frontend: tests that hit the running frontend
